WINNER: Z
RUNNER-UP: X
REASONING: Z dominates because it absorbs X's strongest structural fix (two named columns `vol_regime_market`/`vol_regime_symbol` that "force every consumer to pick one by name") AND incorporates Y's deeper epistemic insight that X misses entirely — the SPY-truth circularity ("The cited 'true' SPY distribution uses the same LOW<8% / HIGH>=20% / SHOCK>=50% scheme as the labeler. The 81.9% in-sample number is therefore not testing whether the *thresholds* are correct"). Z also resolves the direct contradiction between X (parallel columns) and Y (one column only) with a defensible rebuttal — "the current single-column design *already* has a hidden second meaning — broken on 65 symbols — and the train/live gap stems from missing contract documentation, not column count." X's CI check P3 condition (b) "any single symbol's `vol_regime_symbol` distribution has fewer than three of {LOW, NORMAL, HIGH, SHOCK} on a 252-day rolling window" would actually fire false positives on legitimately calm defensive ETFs — Y caught this and Z preserved the correction ("Per-symbol forced distributions would fire false positives on a defensive utility ETF that genuinely sits NORMAL all year").
WINNING_STRENGTH: True synthesis with addition — Z keeps X's named-column fix as the lead recommendation, adds Y's circularity insight as a sixth danger ("Circular in-sample tuning bias... mis-specified at a deeper level than naive overfitting"), explicitly defends against Y's parallel-column objection rather than capitulating, and corrects X's flawed per-symbol CI check using Y's universe-aggregate framing.
RUNNER_UP_GAP: X misses the SPY-truth circularity flaw entirely — its danger #6 says only "81.9% is a model scored on its own training set," treating it as naive overfitting when the deeper issue is that ground truth and labeler share the same threshold scheme, making the validation tautological. X's CI check #7(b) is also subtly wrong (would false-positive on calm defensive ETFs), a flaw Y catches and Z preserves the correction for.

WINNER: Z
