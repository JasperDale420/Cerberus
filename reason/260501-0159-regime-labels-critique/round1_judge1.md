WINNER: X
RUNNER-UP: Z
REASONING: X and Z share the same core insight (the cross-symbol pathology is consistent with absolute thresholds cutting through realized-vol distribution at "the seam separating index-like from single-name-like" — X's phrasing) and both correctly reject Y's lead fix as "destroys semantics" (Y recommends per-symbol percentile thresholds, then proposes a CI guard "fails CI if any symbol's HIGH/SHOCK fraction exceeds 50%" — under percentile labels this metric is circular by construction, a flaw X explicitly flags: "that metric is circular under both threshold regimes and would have been gamed by the percentile fix"). X edges Z by adding a 5th concrete danger ("Universe composition skew disguised as regime ... autoresearch may be inadvertently learning symbol selection disguised as regime conditioning"), by sharper P0/P0-conditional sequencing, and by the explicit anti-circularity note on the CI rule. Y's headline "No. Labeler A's per-symbol parquets are not usable" is more decisive but rests on the lead fix that X and Z show would silently re-interpret HIGH 68 different ways and break activation policies and HRP allocation.
WINNING_STRENGTH: X's lead fix (market-wide `vol_regime` propagated to all symbols + separate `symbol_vol_pctile` column) is both root-cause and preserves the train==live contract with `MarketContextService`, and X's reasoning closes the loop by noting why the obvious-looking alternative (per-symbol percentile) is undetectably worse than status quo.
RUNNER_UP_GAP: Z is substantively almost identical to X but omits the 5th danger (symbol-class-disguised-as-regime), uses slightly weaker P0→P1 conditional gating ("P1, conditional on P0" vs X's "P0, conditional on P0a"), and lacks X's explicit "would have been gamed by the percentile fix" callout that demonstrates awareness of the circularity trap Y fell into.

WINNER: X
